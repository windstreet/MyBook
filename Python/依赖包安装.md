# 包安装

```bash

pip install wordcloud

# 其他方案：
# 在 github 下载 wordcloud 的包，解压缩后，在对应目录中用以下命令安装：python setup.py install


pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple --no-cache-dir

```